# High-Priority Fixes — 2026-07-27

> Authoritative list. Each item says *what*, *why*, *where*, and *verification*. Items in this list are safe and small; see `LOW_PRIORITY_FIXES.md` for backlog.

| # | Severity | Area | Title | Where | What | Why | Verification |
|---|---|---|---|---|---|---|---|
| H1 | **HIGH** | `material_importer` | `_ensure_test` silently un-publishes live tests on rebuild | `backend/material_importer/mock_test_builder.py:79` | In `_ensure_test`, change `Test.objects.update_or_create(title=name, defaults=defaults)` to preserve `is_published` if a previous row exists. Concretely, load the existing row first; if `t.is_published` is True, omit `is_published` from `defaults`. | A second run of `manage.py build_auto_tests --batch N` will reset all auto-tests to `is_published=False`, hiding them from students without notice. | `manage.py build_auto_tests --batch 13` twice, assert `Test.is_published` unchanged for rows already published. |
| H2 | HIGH | `material_importer` | Add unit tests for the new pipeline | `backend/material_importer/tests/test_*.py` (does not exist) | Add at minimum: (a) parser golden tests for 4 DOCX formats (golden fixtures in `tests/fixtures/`); (b) duplicate detector (exact + 6-gram); (c) publishing (idempotency); (d) mock_test_builder (preserves `is_published`). | This app publishes to the live question bank. Zero coverage today. | `python manage.py test material_importer --verbosity=2`. |
| H3 | HIGH | `material_importer` | DOCX namespace error leaves one file unparseable | `backend/material_importer/parser/docx_parser.py:_docx_read` | Wrap `python-docx` parse in try/except; on `Namespace prefix … not defined`, fall back to XML extraction (`docx_zip` → parse `word/document.xml` `<w:t>` text + still extract media files). | Batch #13 reports 1 file (`merged_notes-document (1).docx`) failing out of 103. Total recoverable questions: unknown but non-zero. | `manage.py ingest_cms_material --path cms_exclusive_material/merged_notes-document\ \(1\).docx` returns ≥0 errors. |
| H4 | MEDIUM | `material_importer` | `_seed_existing_dedup` is O(N) per batch | `backend/material_importer/ingest_service.py:76-95` | Serialize the inverted index to `MEDIA_ROOT/_cache/dedup_index.json` after first build; reload on subsequent batches. | First batch of each session blocks the request thread for 2-4 s on an 8 K-bank. | Re-run ingest twice — second call skips reseed. |
| H5 | MEDIUM | `material_importer` | Subject alias resolver scans `Subject.objects.all()` per question | `backend/material_importer/ingest_service.py:110-141` | Rename `Name_or_code` → `_resolve_subject_alias`; cache the alias dict at module load (`@functools.lru_cache` over a function that returns the dict). | 1,000 questions × `SELECT *` × small table = wasteful. | Time `ingest_service._persist_parsed_document` on a 100-Q batch before and after. |
| H6 | MEDIUM | `material_importer` | Enrichment streams with no batch commit | `backend/material_importer/enrichment.py:enrich_batch` | Accumulate rows in a list of 50, then `_apply_enrichment` + `bulk_update` per chunk. Also cache `_call_explain` results keyed by `(question_text, options, correct)` so re-runs are free. | Today: 1,000 enriched = 1,000 round-trips to AI; 1,000 DB writes. | Run twice — second pass should not call AI. |
| H7 | MEDIUM | Fixture loader | Doc-comment rows with `_doc`/`_note` keys can crash `loaddata` if loaded directly | `backend/fixtures/{inicet,neet_pg}_fixture.json` | Either (a) document that `load_exam_fixture` filters `_`-prefix keys before passing to `loaddata`, or (b) move comments out of the JSON array. Verify `load_exam_fixture` actually filters them. | If `manage.py loaddata fixtures/inicet_fixture.json` is ever called, it will reject the dicts. | `manage.py loaddata backend/fixtures/inicet_fixture.json` — confirm clean rejection OR fix the loader. |
| H8 | LOW | Frontend | Tailwind 4 upgrade touched only 4 files; rest may still be Tailwind 3 syntax | `frontend/src/**/*.tsx` | Run `npm run build`; the Tailwind 4 compiler will surface any remaining `[var(--…)]` and arbitrary values that need rewriting. | Bumped 4 files of TypeScript; many more exist. | `npm run build` — zero CSS errors. |
| H9 | LOW | CI | New `material_importer` app needs to be in the CI test path | `.github/workflows/ci.yml` | Confirm `python manage.py test material_importer` runs in CI. If migrations are baked in, no extra steps; if the test depends on data, seed it from fixtures. | Without it, the importer can regress unnoticed. | Open `.github/workflows/ci.yml` and confirm. |
| H10 | LOW | `crack_cms/views.py` | `_ensure_question_bank_loaded` lazy-bootstraps CMS fixtures | `backend/questions/views.py:_ensure_question_bank_loaded` | Add a comment + a log line on first bootstrap so the admin sees when the 132 KB fixture loaded vs when it skipped because already loaded. | The lazy path is undocumented → DB weirdness confuses the team. | Manual: clear DB → restart → observe log. |
